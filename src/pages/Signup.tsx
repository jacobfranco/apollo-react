import React from 'react';
import Signup from '../features/Signup'; // Assuming Signup is a component

const SignupPage: React.FC = () => {
  return (
      <Signup />
  );
};

export default SignupPage;
